#include "api-motion.h"

#include "../motion/MotionManager.h"

// setup function gets server as parameter and a pointer to the outputs struct
void setupApiMotion(AsyncWebServer *server_p, MotionManager *motionManager) {}
